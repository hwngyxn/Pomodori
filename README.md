# Pomodori – A macOS-style Pomodoro Timer

A lightweight, menu-bar-style Pomodoro timer app built with **React**. Designed to simulate the behavior of a macOS menu bar app within a browser environment.